/**
 * Units — pure conversion constants.
 * No imports, no side effects. Extracted from goal.js so distance-derived
 * modules (records, progress) do not pull in the goal engine.
 */

export const KM_TO_STEPS = 1312.33;
